import homes from "@/api/modules/homes";
import agents from "@/api/modules/agents";
import auth from "@/api/modules/auth";
import users from "@/api/modules/users";

const api = {
  homes,
  agents,
  auth,
  users,
};

export default api;
